\ProvidesPackage{example}

\usepackage{graphicx}
\graphicspath{ {./sections/img/} }%guides the compiler to directory where images are stored

\usepackage[framemethod=TikZ]{mdframed}%for boxes around questions
\newcounter{theo}[section]\setcounter{theo}{0}%for counter with each box "theorem"
\renewcommand{\thetheo}{\arabic{section}.\arabic{theo}}%for counter with each box "theorem"
\newenvironment{theo}[2][]{%
    \refstepcounter{theo}
 
    \ifstrempty{#1}%
% if condition (without title)
{\mdfsetup{%
    frametitle={%
        \tikz[baseline=(current bounding box.east),outer sep=0pt]
        \node[anchor=east,rectangle,fill=gray!20]
        {\strut What do you think?};}%
    }%
% else condition (with title)
}{\mdfsetup{%
    frametitle={%
        \tikz[baseline=(current bounding box.east),outer sep=0pt]
        \node[anchor=east,rectangle,fill=gray!40]
        {\strut What do you think?};}%
    }%
}%
% Both conditions
\mdfsetup{%
    innertopmargin=10pt,linecolor=gray!40,%
    linewidth=2pt,topline=true,%
    nobreak=true,%stops questions from breaking across pages inappropriately
    frametitleaboveskip=\dimexpr-\ht\strutbox\relax%
}
 
\begin{mdframed}[]\relax}{%
\end{mdframed}}

\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{enumitem}%for to do list

%\usepackage[latin1]{inputenc}
\usepackage[spanish, english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[letterpaper]{geometry}
\usepackage{subcaption}%for figures
\usepackage{hyperref}%for hyperlinks
\usepackage[font=small,labelfont=bf,]{caption}%for captions
    \captionsetup{format=hang}
\usepackage{placeins}%prevent images from floating into inappropriate sections
\newcommand*{\doi}[1]{\href{http://doi.org/#1}{doi: #1}}% links DOI
\newcommand{\latinword}[1]{\texttt{\itshape #1}}%use \latinword for vocab
\def\labelitemi{--} %change bullet to em dash
\newenvironment{labelfontsmall}{\fontfamily{phv}\selectfont}{\par}%for label font example

\usepackage{natbib}	%for bibliography
    \def\bibfont{\scriptsize}%change font size in bibliography
\usepackage{blindtext}
\usepackage{textcomp}
\usepackage{pgfplots}
\usepackage{float}%force tables/figs into certain placement
\usepackage{changepage}%for dichotomous key

%\usepackage{wrapfig}%to wrap text around brain/light bulb icon

\pgfplotsset{width=10cm,compat=1.9}

\usepackage{titlesec}%here, with commands below, will modify chapter numbers
\definecolor{gray75}{gray}{0.75}
\newcommand{\hsp}{\hspace{20pt}}
\titleformat{\chapter}[hang]{\Huge\bfseries}{\thechapter\hsp\textcolor{gray75}{|}\hsp}{0pt}{\Huge\bfseries}
%\titleformat{\chapter}[block]{\bfseries\huge}{}{0em}{}
%\titleformat{\section}[block]{\bfseries\Large}{}{0em}{}%eliminates number before section
%\titleformat{\subsection}[block]{\bfseries\large}{}{0em}{}%eliminates number before subsection

%Header styles
\usepackage{fancyhdr}
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{#1}{}}
\fancyhf{}
\fancyhead[LE,RO]{\thepage}
\fancyhead[RE]{\textbf{\textit{\nouppercase{\leftmark}}}}
\fancyhead[LO]{\textbf{\textit{\nouppercase{\rightmark}}}}
\fancypagestyle{plain}{ %
\fancyhf{} % remove everything
\renewcommand{\headrulewidth}{0pt} % remove lines as well
\renewcommand{\footrulewidth}{0pt}}

\newlist{todolist}{itemize}{2}
\setlist[todolist]{label=$\square$}

%makes available the commands \proof, \qedsymbol and \theoremstyle
\usepackage{amsthm}

%Ruler
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}

%Lemma definition and lemma counter
\newtheorem{lemma}{Lemma}[section]

%Definition counter
\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]

%Corolary counter
\newtheorem{corolary}{Corolary}[section]

%Example cenvironment
\theoremstyle{remark}
\newtheorem{examle}{Example}

%Example counter
\newcommand{\reiniciar}{\setcounter{example}{0}}

% to make index
\usepackage{imakeidx}
\usepackage[normalem]{ulem}% for strikethrough with command \sout{}